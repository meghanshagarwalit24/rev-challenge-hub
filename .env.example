# Copy this file to .env and fill in your values

# MongoDB connection string (required)
MONGODB_URI=mongodb://user:password@host:port/dbname

# Port the Node.js HTTP server listens on (default: 3000)
PORT=3000
